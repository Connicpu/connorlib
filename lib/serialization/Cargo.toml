[package]
name = "serialization"
version = "0.1.0"
authors = ["Connor Hilarides <connorcpu@live.com>"]

[lib]
crate-type = ["dylib", "staticlib"]

[dependencies]
toml = "0.1"
rustc-serialize = "0.3.19"

